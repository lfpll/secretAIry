import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      {/* Header content - offline sync indicator removed */}
    </header>
  );
};

export default Header; 